

class BudgetList:
    def BudgetList(self, budget):
        self.budget=budget
        self.sum_expenses=0
        self.expenses=[]
        self.sum_overages=0
        self.overages=[]

    def append(self, item):
        check=self.sum_expenses+item
        if check check<self.budget:
            self.expenses.append(item
            self.sum_expenses+=item

    def __len__(self)
        
